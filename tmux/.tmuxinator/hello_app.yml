name: default
root: ~/
windows:
      - zsh:
          layout: main-horizontal
          panes:
              - cd ~/workspace/hello_app
              - cd ~/workspace/hello_app
      - vim:
          panes: 
            - vim ~/workspace/hello_app
      - server:
          panes:
            - ~/workspace/hello_app/bin/rails server
      - console:
          panes:
            - ~/workspace/hello_app/bin/rails console
            
